#!/usr/bin/env bash
#
# Utility for other scripts.
#
# Prints the current feature or bugfix identifier, if any.
#
set -e -o pipefail

branch=$(git rev-parse --abbrev-ref HEAD)
if [[ "${branch}" =~ ^(feature|bugfix)\/(.*) ]]; then
    echo "${BASH_REMATCH[2]}"
fi
